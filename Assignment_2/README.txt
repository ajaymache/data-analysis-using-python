Please Specify the file name followed by a space and the desired search term.
  
  Example : In the command window type the following:
            
            python Assignment2_DAUP.py search_term
            
            where 'search_term' is the actual search term.
            
            
The data file is downloaded and saved with the same file name wih an extension '.txt' in the same directory where the script 
is downloaded. 

In this case: The downloaded file can be found by the name 'search_term.txt'

Once you run the script the Analysis are printed on the console
  
          
          
          
